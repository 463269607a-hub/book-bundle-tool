# 画布 800×800，原点左上角
# scale_h:  书高占画布比例（×800 = 像素高度）
# max_w:    该位置允许的最大宽度（像素）。书太宽时改按宽度缩放，避免霸占整行。
#           同系列开本一致的书一般不会触发；只约束异常宽（正方形/横开本）的书。
# cx:       书图水平中心
# baseline: 书图底边的 y 坐标（同一排用同一条基线 → 底部对齐，立在书架上）
# z:        绘制顺序，越大越靠前（后画、压上层）

TEMPLATES = {
    # ── 2本：并排，底边对齐，前书压后书右侧 ─────────────────────
    2: [
        {"scale_h": 0.80, "max_w": 480, "cx": 500, "baseline": 725, "z": 2},  # code_1 前书
        {"scale_h": 0.80, "max_w": 480, "cx": 290, "baseline": 725, "z": 1},  # code_2 后书
    ],

    # ── 3本：后1（稍大）+ 前2（底边对齐）金字塔 ─────────────────
    3: [
        {"scale_h": 0.70, "max_w": 420, "cx": 400, "baseline": 562, "z": 1},  # code_1 后排居中
        {"scale_h": 0.70, "max_w": 420, "cx": 250, "baseline": 765, "z": 2},  # code_2 前排左
        {"scale_h": 0.70, "max_w": 420, "cx": 550, "baseline": 765, "z": 3},  # code_3 前排右
    ],

    # ── 4本：上下两排「同宽」（按宽度统一），底边各自对齐 ──────────
    # scale_w 让四本同宽，不受各书宽高比影响；max_h 防止过高溢出
    4: [
        {"scale_w": 0.42, "max_h": 485, "cx": 245, "baseline": 490, "z": 1},  # code_1 后排左
        {"scale_w": 0.42, "max_h": 485, "cx": 555, "baseline": 490, "z": 2},  # code_2 后排右
        {"scale_w": 0.42, "max_h": 485, "cx": 245, "baseline": 780, "z": 3},  # code_3 前排左
        {"scale_w": 0.42, "max_h": 485, "cx": 555, "baseline": 780, "z": 4},  # code_4 前排右
    ],

    # ── 5本：后排2（高、小）+ 前排3（底边对齐、依次叠压）────────
    5: [
        {"scale_h": 0.54, "max_w": 350, "cx": 290, "baseline": 460, "z": 1},  # code_1 后排左
        {"scale_h": 0.54, "max_w": 350, "cx": 510, "baseline": 460, "z": 2},  # code_2 后排右
        {"scale_h": 0.60, "max_w": 380, "cx": 200, "baseline": 778, "z": 3},  # code_3 前排左
        {"scale_h": 0.60, "max_w": 380, "cx": 400, "baseline": 778, "z": 4},  # code_4 前排中
        {"scale_h": 0.60, "max_w": 380, "cx": 600, "baseline": 778, "z": 5},  # code_5 前排右
    ],
}
